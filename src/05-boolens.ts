(() => {

  let isEnable = false;


  isEnable = true;

  let isNew: boolean = true;
  console.log(isNew);

  isNew = false;
  console.log(isNew);

  const random = Math.random();
  console.log(random);

  isNew = random > 0.5;

  console.log(isNew);
})
